import { ISetTargetAutomationEvent } from 'automation-events/src/interfaces';
import { TAutomationEvent } from 'automation-events/src/types';

export const isSetTargetAutomationEvent = (automationEvent: TAutomationEvent): automationEvent is ISetTargetAutomationEvent => {
    return automationEvent.type === 'setTarget';
};
