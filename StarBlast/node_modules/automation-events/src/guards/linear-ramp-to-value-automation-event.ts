import { ILinearRampToValueAutomationEvent } from 'automation-events/src/interfaces';
import { TAutomationEvent } from 'automation-events/src/types';

export const isLinearRampToValueAutomationEvent = (
    automationEvent: TAutomationEvent
): automationEvent is ILinearRampToValueAutomationEvent => {
    return automationEvent.type === 'linearRampToValue';
};
